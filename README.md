# Algorithm
This project implements Aihara’s algorithm for efficiently handling a sequence of mutations. The algorithm updates only the affected parts of the structure instead of recomputing it from scratch. The project includes a Python implementation and a comparison with an alternative algorithm, focusing on efficiency and time complexity.
